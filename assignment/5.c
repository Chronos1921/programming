#include <stdio.h>
int main()
{
    int n, i;
    printf("Enter the number : ");
    scanf("%d", &n);
    for (i = 2; i < n / 2; i++)
    {
        while ((n/2)%i!=0)
        {
          
        }
        
    }

    return 0;
}